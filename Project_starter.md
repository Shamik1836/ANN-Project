In this project we are given a dataset and we need to predict i the person will be leaving a bank or not

Churn Modelling Dataset -> Classification -> Basic Feature Enginnering ( Convert categorical variables into numerical values), Standardization -> Create an ANN

We will also learn to drop out features so that our model doesn't overfit


Code will be using Keras and tensorflow

Model -> File Format (pickle or .h5 format file), for reusing and deploying -> use streamlit create web app deploy it in streamlit cloud


ann is sequential model